<td class="commend">
    <span class="content"><%= commend.content %></span><br/>
    <span class="timestamp">
	Posted at <%= commend.created_at %>
	<% if current_user == commend.user %>
	  <%= link_to "Delete",commend, :method => :delete,
						  :confirm => "Are you sure?",	
						:title => commend.content %>
