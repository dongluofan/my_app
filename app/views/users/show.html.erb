
<table class="profile">
  <tr>
    <td class="main">
	<h1><%= @user.name %></h1>
	  <% if !@user.microposts.nil? %>
	    <table class="microposts">
	      <% @microposts.each do |micropost| %>

  </tr>
	<tr>
  	<td class="micropost">
   	<h3><span class="title">Title: <%= micropost.title %></span><br/>
   	 <span class="content"><%= micropost.content %></span><br/></h3>
	 <%if micropost.commends.count!= 0%>
	   <% micropost.commends.each do |commend|%>
	   </tr>
  	   <td class="commend">

	   <div style="text-indent:2em">
   	    <span class="content">Comment:<%= commend.content %></span></div>
		<div style="text-indent:2em">
		<%= "commented by "%>
		<%= link_to commend.user.name,user_path(commend.user)%>
   	   
	    <span class="timestamp">
	        at <%= commend.created_at %>
	   <% if current_user == commend.user %>
	         <%= link_to "Delete",commend, :method => :delete,
						  :confirm => "Are you sure?",	
						:title => commend.content %><br/></div>
	   <% end %>
	  <% end %></div>
	<% end %>
	<br/>
   	 <span class="timestamp">
	Posted at <%= micropost.created_at %><br/> <%= micropost.commends.count%>
	<%="comments"%>
	<% if current_user == micropost.user %>
	  <%= link_to "Delete",micropost, :method => :delete,
						  :confirm => "Are you sure?",	
						:title => micropost.content %>
	<% end %>
	<% if current_user!=nil %>
	   <%= form_for([micropost,@commend]) do |f|%>
	    <div class="field">
 	    <%= f.text_field :content %></div>
	      <button class = "btn" type="submit">
  		 Comment
	      </button>
	      <% end %>
             <% end %>
             </span>
            </td>
          </tr>
	<% end %>	   
     <% end %>
   </td></table>
    <td class="sidebar round">
	<strong>Name</strong> <%= @user.name %><br/>
	<strong>Email</strong> <%= @user.email %><br/>
	<strong>URL</strong> <%= link_to user_path(@user),@user %><br/>
	<strong>Microposts</strong> <%= @user.microposts.count %>
	<% if current_user == @user %>
	  <br/>
	
	  <%= link_to "Edit My Count", edit_user_path(@user) %>
	<% end %>
    </td>
  </tr>
</table>
<% if current_user.nil? %>
<%= link_to 'Sign In to comment', signin_path %>&nbsp&nbsp&nbsp&nbsp&nbsp
<% end %>
<%= link_to 'Back', users_path %>






